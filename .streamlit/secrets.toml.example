# Streamlit Secrets Configuration Example
# Copy this file to secrets.toml and configure with your actual values

# Authentication Control
# Set to true to temporarily disable authentication (useful for development/testing)
# WARNING: NEVER set this to true in production!
DISABLE_AUTH = false

# Environment
# Options: "dev", "staging", "production"
ENV = "dev"

# Supabase Configuration (if using)
# [supabase]
# url = "your-supabase-url"
# key = "your-supabase-anon-key"

# Google OAuth (if using custom OAuth instead of Streamlit native)
# [oauth]
# client_id = "your-google-oauth-client-id"
# client_secret = "your-google-oauth-client-secret"

# Other API Keys
# Add your API keys here as needed